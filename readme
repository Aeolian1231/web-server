webserver_epoll.cpp:实现基于 epoll 的 Reactor

# 统一事件源设计（超时检查）：
主循环通常阻塞在 epoll_wait 上，等待网络数据。

如果不设超时（timeout = -1）：如果没有网络请求进来，epoll_wait 会永久阻塞。此时，即使某个连接的 60 秒定时器到期了，主线程还在睡觉，没法去关闭那个连接。
如果设死超时（比如 timeout = 10ms）：主线程每 10 毫秒就醒一次。即使没有任务，它也会疯狂空转，白白消耗大量 CPU。

动态计算超时：根据最快到期的任务时间，决定主线程的睡眠时间

# ET 模式的特点：
如果内核队列里进来了 5 个连接，Epoll 只会通知你一次。
如果你只调用一次 accept 就走了，剩下的 4 个连接就会一直堆积在内核里，Epoll 不再发第二次通知。

对策：必须用 while(true) 循环不停地 accept，直到把内核队列完全榨干（直到看到 EAGAIN），才算处理完毕。

# readIntoBuffer 读入缓冲区
扫描缓冲区，寻找 HTTP 协议的结束标志 —— \r\n\r\n

如果找到了 \r\n\r\n：它会返回从开头到这个标志的总长度（比如 150 字节）。这代表我们拿到了一个完整的 HTTP 请求头。
如果没找到 \r\n\r\n：它会返回 0。这并不代表没读到数据，而是代表“目前缓冲区里的数据还构不成一个完整的 HTTP 头部”。

一次循环没有找到结束标志会等待下一次接收，直到缓冲区里有足够的数据发送给worker线程